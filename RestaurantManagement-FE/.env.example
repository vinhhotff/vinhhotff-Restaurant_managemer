# Backend API base URL (no trailing slash)
# CORS: Backend (SecurityConfig) cho phép origin http://localhost:3000. Chạy FE trên port 3000 để gọi Backend không lỗi CORS.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
